# Intentionally (almost) empty. The mere presence of this file marks the pytest
# rootdir and puts the repo root on sys.path, so `import benchmark` resolves in the
# test suite without any install or PYTHONPATH tweaks. Add shared fixtures here only
# if they are genuinely needed across multiple test modules.
